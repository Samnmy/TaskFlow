spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/taskflow
    username: postgres
    password: 6466O
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

app:
  jwt:
    secret: CambiaEsteSecretoPorUnoMuyLargoYSeguroEnProduccion2026SuperSecureAndLongEnoughForHS512TaskFlowProject
    expiration-ms: 86400000 # 24 Hours
